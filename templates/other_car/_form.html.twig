{{ form_start(form) }}

<table>
    <tr>
        <td>{{ form_label(form.brand) }}</td>
        <td>{{ form_widget(form.brand) }}</td>
    </tr>
</table>

    {{ form_row(form.name) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
